The `HiveRepository` is a repository-based wrapper around [`hive`](https://pub.dev/packages/hive).

You still need to register `TypeAdapters` for the classes that you want to save
as well as initialize Hive.
